<?php

namespace Domain\{{ domain }}\Models;

use FilippoToso\Domain\Traits\Domainable;
use Illuminate\Database\Eloquent\Model;
// use Domain\{{ domain }}\States\{{ name }}State;
// use Spatie\ModelStates\HasStates;

class {{ name }} extends Model
{
    use Domainable;
    // use HasStates;

    protected $dispatchesEvents = [
    ];

    /*
    public function getStateAttribute(): {{ name }}State
    {
        return new $this->state_class($this);
    }
    // */
}
